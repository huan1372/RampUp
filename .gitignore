# OpenClaw config contains API keys — never commit
.openclaw/config.yml
.openclaw/workspace-state.json

# Memory files (personal, not for repo)
memory/

# Local tool state — not for repo
.claude/
.obsidian/

# OS files
.DS_Store
Thumbs.db
